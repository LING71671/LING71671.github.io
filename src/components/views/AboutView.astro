---
import { Image } from 'astro:assets';
import { SITE, SOCIALS } from '../../config/site';
import avatar from '../../assets/avatar.jpg';
---

<section class="about">
  <div class="page-head">
    <h1>关于我</h1>
  </div>
  <div class="profile card">
    <Image src={avatar} alt={`${SITE.name}的头像`} width={96} height={96} class="avatar" />
    <div class="who">
      <h2>{SITE.name}</h2>
      <p class="roles">{SITE.roles.join(' / ')}</p>
      <p class="bio">{SITE.bio}</p>
    </div>
    <ul class="contacts">
      {
        SOCIALS.map((s) => (
          <li>
            <span class="k">{s.label}</span>
            {'href' in s && s.href ? (
              <a href={s.href} target={s.href.startsWith('http') ? '_blank' : undefined} rel="noopener">
                {s.value}
              </a>
            ) : (
              <span>{s.value}</span>
            )}
          </li>
        ))
      }
      <li>
        <span class="k">城市</span>
        <span>{SITE.location}</span>
      </li>
    </ul>
    <a class="contact-btn" href="https://github.com/LING71671" target="_blank" rel="noopener">GitHub 上找我</a>
  </div>
  <div class="prose about-body">
    <p>
      你好，我是{SITE.name}。平时的工作是把别人的程序拆开看：读反编译出来的代码，写自动化分析的工具，整理逆向工程的知识库。这个网站是工作之外做的一件小东西。
    </p>
    <p>
      一个写代码的人做一张 3D 书桌，理由其实很简单：想知道浏览器里的三维场景到底能不能既好看又能用。所以这里的分工很清楚，Three.js 只负责氛围和导航，每一篇文字都是最朴素的
      HTML，可以被检索、被链接、被慢慢读。
    </p>
    <p>笔记里写的多是具体的活：source map 考古、并发提交的坑、MCP 工具链的取舍。如果哪一篇对你有用，或者你发现我写错了，欢迎在 GitHub 上找我。</p>
  </div>
</section>

<style>
  .profile {
    padding: var(--space-6);
    display: grid;
    grid-template-columns: auto 1fr;
    gap: var(--space-5);
    align-items: start;
  }

  :global(.about .avatar) {
    border-radius: 50%;
    border: 2px solid var(--brass);
  }

  .who h2 {
    font-size: var(--text-2xl);
    font-weight: 700;
  }

  .roles {
    color: var(--brass);
    font-size: var(--text-sm);
    margin-top: var(--space-1);
  }

  .bio {
    color: var(--ink-soft);
    margin-top: var(--space-2);
  }

  .contacts {
    grid-column: 1 / -1;
    list-style: none;
    padding: var(--space-4) 0 0;
    border-top: 1px solid var(--line-soft);
    display: grid;
    gap: var(--space-2);
  }

  .contacts li {
    display: flex;
    gap: var(--space-4);
  }

  .contacts .k {
    flex-shrink: 0;
    width: 4em;
    color: var(--ink-faint);
    font-size: var(--text-sm);
  }

  .contact-btn {
    grid-column: 1 / -1;
    justify-self: start;
    background: var(--brass);
    color: var(--paper);
    border-radius: var(--radius-sm);
    padding: 0.5em 2em;
    font-size: var(--text-sm);
  }

  .contact-btn:hover {
    background: var(--brass-bright);
    color: var(--paper);
    text-decoration: none;
  }

  .about-body {
    margin-top: var(--space-6);
  }

  @media (max-width: 560px) {
    .profile {
      grid-template-columns: 1fr;
    }
  }
</style>
